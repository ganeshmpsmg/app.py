# Image Classification Web App (Flask + TensorFlow)

This repository contains a complete Flask web application that uses a **pretrained MobileNetV2 model** to classify uploaded images.  
The project includes **login**, **dashboard**, **image upload**, and **AI prediction** features.

---

## 📁 Project Files


---

## ⚙️ Installation (Run Locally)

Install required Python libraries:

```bash
pip install flask tensorflow pillow numpy
python app.py
http://127.0.0.1:5000

Username: Ganesh
Password: 1234
